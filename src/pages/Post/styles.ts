import styled from "styled-components";
import { HomeContainer } from "../Home/styles";

export const PostContainer = styled(HomeContainer)``

export const DetailsCard = styled.div`
    display: flex;
    flex-direction: column;
    padding: 2.5rem 2rem;
    width: 100%;
    height: 422px;
`